subscription {
  Prod(filter: {
    mutation_in: [CREATED, UPDATED, DELETED]
  }) {
    mutation
    node {
      model {
        id
      }
    }
    previousValues {
      id
      model {
        id
      }
    }
  }
}
